import React from "react";

export default function NavBar() {
    return (
        <nav className="flex flex-row justify-center items-center bg-sky-700 h-50">
            <div className="font-mono text-6xl font-bold py-4">FLOP</div>
        </nav>
    );
}
